# PATH: .env.local  (copy this file and fill in your values)
# NEVER commit .env.local to git — it contains secrets.
# For production use Vercel Environment Variables or Doppler.

# ─── Supabase ─────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ─── Gemini AI ────────────────────────────────────────────────────────────────
GEMINI_API_KEY=AIzaSy...

# ─── Email (Resend) ───────────────────────────────────────────────────────────
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=alerts@yourdomain.com

# ─── Upstash Redis (rate limiting in production) ──────────────────────────────
# Get from https://console.upstash.com — free tier is enough for most projects
# Leave blank in development — falls back to in-memory rate limiting.
UPSTASH_REDIS_REST_URL=https://...upstash.io
UPSTASH_REDIS_REST_TOKEN=AX...

# ─── App ──────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ─── Development auth shortcut ───────────────────────────────────────────────
# Set this to any string to skip JWT verification in development.
# The API routes will return this value as the current user ID.
# DELETE this variable (or leave blank) before deploying to production.
DEV_USER_ID=dev-user-local-001
